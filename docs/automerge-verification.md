# Auto-merge verification (temporary)

Throwaway doc-only artifact used to verify that admin-enabled auto-merge waits
for required CI and merges with **no review**, while the required-review rule
still applies to non-admins. This file is removed immediately after the test.
