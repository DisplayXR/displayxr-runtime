<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
        Copyright 2020-2024, Collabora, Ltd.

        SPDX-License-Identifier: BSL-1.0
    -->

    <!-- We may try to use OpenGL|ES 3.0 -->
    <uses-feature
        android:glEsVersion="0x00030002"
        android:required="true" />

    <!-- This is for Android's VR mode. -->
    <uses-feature
        android:name="android.software.vr.mode"
        android:required="false" />

    <!-- This feature is available on only Daydream-ready devices: may use it if it's there. -->
    <uses-feature
        android:name="android.hardware.vr.high_performance"
        android:required="false" />

    <!-- Can handle both 3-DOF and 6-DOF head tracking. -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false"
        android:version="1" />

    <!-- for sensor usage -->
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.hifi_sensors"
        android:required="false" />

    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"
        tools:ignore="HighSamplingRate" />

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- #1245: restart the runtime service after a reboot so a client's
         bindService() always finds a live service (background-restricted ROMs
         refuse to CREATE one on a client's behalf). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        Android 11+ package visibility: the out-of-process runtime SERVICE
        hosts the vendor display-processor plug-in (e.g. the Leia CNSDK),
        whose loader calls PackageManager.getPackageInfo() on the on-device
        LeiaSR services to bind them for display config + face tracking. Under
        package visibility those packages are invisible unless declared here —
        without this the CNSDK loader gets a NameNotFoundException it doesn't
        clear, and the next JNI call trips CheckJNI → the service process
        aborts (no tracked eyes). In-process apps declare these in their own
        manifest; the OOP service must declare them itself. (#510 M2)
    -->
    <queries>
        <package android:name="com.leialoft.display.config" />
        <package android:name="com.leia.headtrackingservice" />
        <package android:name="com.leiainc.media.service" />
    </queries>

    <application
        android:name="org.freedesktop.monado.openxr_runtime.MonadoOpenXrApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            Which CNSDK loader this APK bundles, stamped at build time from the
            CNSDK drop's VERSION.txt (build.gradle). The loader exact-matches the
            Leia plug-in's apiVersion and silently falls back to 2D on mismatch
            (#1357), so make the pair checkable on device:
              adb shell dumpsys package org.freedesktop.monado.openxr_runtime.out_of_process \
                | grep CNSDK_LOADER_VERSION
            "none" = built CNSDK-free (sim_display path). Mirrors the services'
            own com.leia.cnsdk.FULL_VERSION meta-data (CNSDK >= 0.10.65).
        -->
        <meta-data
            android:name="com.displayxr.CNSDK_LOADER_VERSION"
            android:value="${cnsdkLoaderVersion}" />
        <!--
            Which BUILD of that loader (x.y.z+<n>.<sha>, from the CNSDK drop /
            release zip name). Identification only, NOT asserted against the
            binary, which carries no build metadata. Two builds of one dotted
            version are indistinguishable by CNSDK_LOADER_VERSION; this is what
            tells them apart. "unknown" = drop had no build info (local dev).
        -->
        <meta-data
            android:name="com.displayxr.CNSDK_LOADER_BUILD"
            android:value="${cnsdkLoaderBuild}" />

        <!--
            Diagnostics dashboard (#558) replaces the shared android_common
            AboutActivity as this APK's launcher: on-device displayxr-cli
            info/selftest snapshot + live service state + the permission
            fragments hosted as proper cards.
        -->
        <activity
            android:name="org.freedesktop.monado.android_common.AboutActivity"
            tools:node="remove" />
        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The diag query runs in its own short-lived process: the native
            side skips runtime teardown (vendor plug-in destroy can hang,
            displayxr-leia-plugin#39) and the service self-kills after each
            reply, so a hang can never wedge the dashboard.
        -->
        <service
            android:name=".DiagQueryService"
            android:exported="false"
            android:process=":diag" />

        <!-- VR Listener service, for interop with Android VR mode. -->
        <service
            android:name="org.freedesktop.monado.android_common.MonadoVrListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VR_LISTENER_SERVICE">
            <meta-data
                android:name="org.khronos.openxr.OpenXRRuntime"
                android:value="libopenxr_displayxr.so" />
            <intent-filter>
                <action android:name="android.service.vr.VrListenerService" />
            </intent-filter>
        </service>


        <receiver
            android:name="org.freedesktop.monado.ipc.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <service
            android:name="org.freedesktop.monado.android_common.RuntimeService"
            android:exported="true"
            android:label="@string/service_name"
            tools:ignore="ExportedService">
            <meta-data
                android:name="org.khronos.openxr.OpenXRRuntime.SoFilename"
                android:value="libopenxr_displayxr.so" />
            <meta-data
                android:name="org.khronos.openxr.OpenXRRuntime.MajorVersion"
                android:value="1" />

            <intent-filter>
                <action android:name="org.khronos.openxr.OpenXRRuntimeService" />
            </intent-filter>
        </service>

        <!--
            Khronos OpenXR loader runtime-broker ContentProvider.
            The loader's Android discovery path queries this authority
            on first xrCreateInstance to find out which runtime .so to
            dlopen — without it the loader fails with
            XR_ERROR_RUNTIME_UNAVAILABLE even when the RuntimeService
            above is installed (the loader doesn't fall back to the
            service-intent discovery on its own — broker is the
            primary mechanism on Android-12+).
        -->
        <provider
            android:name="org.freedesktop.monado.openxr_runtime.OpenXRRuntimeBroker"
            android:authorities="org.khronos.openxr.runtime_broker"
            android:exported="true" />
            <!--
            Weave satellite occlusion feed (#1277 P1). An AccessibilityService
            is the only app-reachable source of other windows' rects + z-order;
            the satellite clips the weave under overlapping 2D windows with it.
            Opt-in (accessibility settings / adb on a dev box); disabled means
            no occlusion, nothing else changes.
        -->
        <service
            android:name=".WindowWatcherService"
            android:exported="false"
            android:label="DisplayXR Window Watcher"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/dxr_window_watcher" />
        </service>
    </application>

</manifest>
